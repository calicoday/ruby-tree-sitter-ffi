# hacky hacky hacky -- generated by src/gen/rusty_gen.rb, NOT hand-tweaked

# this is a simplistic translation-by-regexp to ruby of the tree-sitter test:
# tree-sitter/cli/src/tests/<%= @boss %>_test.rs
<%# # <%= @srcfile % > # hardcode this to the tree-sitter repo path fragment
so that's never the sole diff. %>

<%# comment something something %>
<% @testdefs.each do |m, guts, skip| %>
<% if skip %><%= "=begin\n" %><% end %>
def <%= m %>
    <%= guts %>
end
<% if skip %><%= "=end\n" %><% end %>

<% end %>

